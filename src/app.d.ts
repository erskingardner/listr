declare global {
    namespace App {
        interface Settings {
            theme: string;
        }

        // interface Errors {}
        // interface Locals {}
        // interface PageData {}
        // interface Platform {}
    }
}

export {};
